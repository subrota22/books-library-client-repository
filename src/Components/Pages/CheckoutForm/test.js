 function find_max(nums) {
    let max_num = Number.NEGATIVE_INFINITY; // smaller than all other numbers
     for (let num of nums) {
    if (num > max_num) {
     // (Fill in the missing line here)
     console.log( max_num = num  ); // x = x + 1 
    }
     }
    return max_num;
     }
     find_max([5 , 8 , 20 , 10]) ;